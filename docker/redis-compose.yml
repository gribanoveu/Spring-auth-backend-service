version: '3.7'

services:
  redis-db:
    image: redis:7.2.1-alpine
    container_name: auth_service_db_redis
    restart: always
    ports:
      - "6379:6379"
    command: redis-server --save 20 1 --loglevel warning
    volumes:
      - cache:/data

volumes:
  cache:
    driver: local